schema_version = "1.0.0"

id      = "efx_editor"
type    = "add-on"

name     = "MHW EFX Editor"
tagline  = "Import and export Monster Hunter World EFX effect files"
version  = "0.2.95"

maintainer = "Dimcirui"

blender_version_min = "4.3.0"

license = ["SPDX:GPL-3.0-or-later"]

# 标签（在扩展浏览器中显示）
tags = ["Import-Export"]

# 扩展包根目录相对于 blender_manifest.toml 的子目录说明：
# 打包正式扩展时应把 blender_efx/ 和 efx_format/ 都放入包根目录，
# 并在下方 [build] 的 paths 中声明（开发期本地安装可省略）。
#
# [build]
# paths = ["blender_efx/", "efx_format/"]
